'use strict';

const discord_js = require('discord.js');

function createTextDisplay(content, id) {
  return new discord_js.TextDisplayBuilder({
    content,
    id
  });
}

exports.createTextDisplay = createTextDisplay;

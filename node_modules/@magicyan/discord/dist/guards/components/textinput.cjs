'use strict';

const discord_js = require('discord.js');
const utils = require('../utils.cjs');

function isTextInputBuilder(value) {
  return value instanceof discord_js.TextInputBuilder || utils.hasConstructor(value) && value.constructor.name === discord_js.TextInputBuilder.name;
}

exports.isTextInputBuilder = isTextInputBuilder;

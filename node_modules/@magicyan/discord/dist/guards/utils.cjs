'use strict';

const core = require('@magicyan/core');

function hasConstructor(value) {
  return typeof value === "object" && core.isDefined(value) && "constructor" in value && typeof value.constructor?.name === "string";
}

exports.hasConstructor = hasConstructor;
